/*#include <iostream>
#include <string>

using namespace std;

int main() {

	setlocale(LC_ALL, "Rus");

	char symbol_element = 'п';
	int row_counter = 0;
	int char_counter = 0;
	char test_string[] = "последовательная обработка символов строки в С++ программирование на С++ количество строк#";

	while (test_string[char_counter] != '#') {
		
		if (test_string[char_counter] == symbol_element) {
			row_counter++;
		}

		char_counter++;
	}

	std::cout << "Количество повторений символа " << "'" << symbol_element << "'" << "= " << row_counter << endl;

	return 0;
}
*/