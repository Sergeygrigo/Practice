/*
#include <iostream>
#include <cmath>

using namespace std;

int main() {

	setlocale(LC_ALL, "Rus");

	float value_X;
	float value_Y;
	float value_Z;
	float sum_pows;
	float vector_length;

	cout << "Введите параметр X: ";
	cin >> value_X;
	cout << "Введите параметр Y: ";
	cin >> value_Y;
	cout << "Введите параметр Z: ";
	cin >> value_Z;

	cout << "Вы ввели X = " << value_X << ", Y = " << value_Y << ", Z = " << value_Z << endl;

	sum_pows = pow(value_X, 2) + pow(value_Y, 2) + pow(value_Z, 2);
	vector_length = sqrt(sum_pows);

	cout << "Длина вектора = " << vector_length << endl;

	return 0;
}
*/